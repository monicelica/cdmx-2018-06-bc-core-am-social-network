describe('', () => {
  it('Debería exponer función Guardar en objeto global', () => {
    assert.isFunction(guardar);
  });

  it('Debería exponer función Eliminar en objeto global', () => {
    assert.isFunction(eliminar);
  });

  it('DSebería exponer función Editar en objeto global', () => {
    assert.isFunction(editar);
  });
});


             
    
  
  